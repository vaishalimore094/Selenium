package Hard_assertstudy;

import org.testng.Assert;
import org.testng.Reporter;
import org.testng.annotations.Test;

public class Assert_equalsmethod {
  @Test
  public void tc1Assertequals()
  {
	 String s1="velocity";
	 String s2="velocity1";	     //this msg shows only fail msgs//    
	 Assert.assertEquals(s1, s2,"Actual and expected not Matching");//---failed not matching s1&s2 
	 
	 //if else use then increace no of lines and execution time
	// if(s1.equals(s2))
			 {
	//	 System.out.println("both are matching tc is passed");
			 }
		// else 
		// {
		//	 System.out.println("both are not matching tc failed");
		// }
			 
      Reporter.log("s1 and s2 are matching ",true);//---if matching then pass this result otherewise
      //stuck in that line no 13.
  }
}
